@model IEnumerable<FinalAssignmentBrief.Models.Course>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DurationHours)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fees)
            </th>
            
            <th>Department</th>
            <th>Instructors</th>
            <th>Students Enrolled</th>
            <th>Level</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.Title)
            </td>
            <td>
                @Html.DisplayFor(model => item.Description)
            </td>
            <td>
                @Html.DisplayFor(model => item.DurationHours)
            </td>
            <td>
                @Html.DisplayFor(model => item.Fees)
            </td>
                <td>
                    @item.Department?.Name
                </td>

                <td>
                    @string.Join(", ", item.Instructors.Select(i => i.FullName))
                </td>

                <td>
                    @item.Enrollments.Count
                </td>
                <td>
                    @item.Level
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
